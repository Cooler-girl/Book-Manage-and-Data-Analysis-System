@using BookMS.Models.ViewModels
@model ResendEmailConfirmationModel
@{
    ViewData["Title"] = "重发验证邮件";
}

<h1 class="text-center text-muted" style="margin-top:3%">@ViewData["Title"]</h1>
<h2 class="text-center text-muted" style="margin-top:3%">输入您的邮箱.</h2>
<hr />
<div class="row justify-content-center align-items-center">
    <div class="col-md-4">
        <form method="post" asp-action="ResendEmailConfirmation">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-floating">
                <input asp-for="Input.Email" class="form-control" aria-required="true" />
                <label asp-for="Input.Email" class="form-label">邮箱</label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary" style="margin-top:2%">发送</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
