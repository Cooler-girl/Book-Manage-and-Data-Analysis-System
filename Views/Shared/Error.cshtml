@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">错误</h1>
<h2 class="text-danger">传递请求时出现错误！</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>请求 ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>开发模式</h3>
<p>    
    切换到<strong>Development</strong>环境可以展示更多出现错误的详细信息。
</p>
<p>
    <strong>不应为已部署的应用程序启用开发环境。</strong>
    它可能导致向最终用户显示来自异常的敏感信息。
    对于本地调试，通过将 <strong>ASPNETCORE_ENVIRONMENT</strong> 环境变量设置为 <strong>Development</strong> 来启用 <strong>Development</strong> 环境并重新启动应用程序。   
</p>
